# Domain Context

## Ignore 条目

通过 `-i` 传入的、相对项目根的排除声明，决定哪些 `.py` 不参与编译。

- 包含：文件条目（精确匹配单个相对路径）、目录条目（以路径分隔符结尾，递归排除其下全部文件，可带 `./` 前缀）
- 不包含：项目外的路径与根自指条目（`/`、`./` 等被拒绝而非静默生效）；文件条目 `main.py` 不排除 `pkg/main.py`
- 关系：被排除的 `.py` 不会被丢弃，转入非编译文件集合原样交付

## 非编译文件

项目中不参与编译、原样拷贝进 `result/` 以保证工程可直接部署的文件。

- 包含：全部非 `.py` 文件、`__init__.py`、被 Ignore 条目排除的 `.py`
- 不包含：`.pyc` 缓存；已进入编译集的 `.py` 源码（源码保护的本意即不让其出现在产物中）
- 关系：与编译集互补，二者共同覆盖项目全部文件

## 产物硬化

编译期削减 `.so` 产物中逆向路标的处理，是工具对外的核心承诺。

- 包含：剥离符号与调试信息、不嵌入函数签名、docstring 不编入二进制
- 不包含：字符串字面量加密（明确不做）；它不是加密——原生逆向仍可分析，只抬高成本
- 关系：代价是被保护程序的运行时自省（`__doc__`、`help()`、签名）不可用，这是预期行为

## result 镜像树

`result/` 内镜像源码相对工作目录原始布局的目录树，编译产物与非编译文件共用同一棵树。

- 包含：相对路径 `-d` 的同构镜像；绝对路径时根/盘符转为一个点目录段（如 `result/.home/u/proj/…`）的镜像
- 不包含：扁平化布局；`build/`、`tmp_build/` 中间目录的内容
- 关系：`.so` 命名会去掉架构与 ABI 标签段（`mod.cpython-311-x86_64-linux-gnu.so` → `mod.so`）
